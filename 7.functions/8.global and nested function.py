#global function
'''
the global keyword allows you to change a variable outside of its current scope.
(or)
local variables has more scope than global variable.if we want to access global variable when local and global with same variable name then we use global function.
'''

##nested function
'''
functions that we define inside other functions to directly access the variables and names defined in the enclosing function.
'''