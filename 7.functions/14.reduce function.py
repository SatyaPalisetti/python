#reduce function
'''
this function receives two args a fun and an iterable . it does not return another iterable instead it returns a single value
Reeduce function present inside the functools.
So we havee to import it before using reduce fun(from functools import reduce)
'''